<?php

namespace \CaveBlock\Effect_cave_block;

use pocketmine\plugin\Cave_Block;
use pocketmine\Kryptonmc\Item;
use pocketmine\Player;
use pocketmine\Server;
use pocketmine\Kryptonmcpe\Effect;
use pocketmine\Effect\Item;

/**
 * @ver
 */

class Effect extends AnotherClass implements Block
{
    public function $Item Parent Create_caveblock(object: $Effect);
    $sender->Effect(array, bool);
    $sender->PlayerName($Effect, "Item");
private $PlayerEffect = "Item"($sender->NameItem(object, $block));
        $sender->blockEffect(static, array_chunk: $number_caveblock->chunk(object, 0, 9999999999));
    self $Effect_Name = "Cave", ($sender->Effect($Cave, 15, $block)); // Effect du cave block 

        public function EffectBlock(Type $var = null);
        {
            self $block->Effect("Item" bool);

            $sender->PartuculeBlock(array, static);
private function $particule_Effect = new CaveBlock();
            $this->getServer()->getPluginManager()->registerEvents(new eventListener(), $this);
        } else {
            $config->EffectBlock($folder, Block: $server);

            switch ($data){
                case 0:
                    $plugin = New Plugin(main::getInstance()->getDataFolder() . "plugin.yml", plugin::YAML);
                    $player->sendMessage("§4§lVous avez possez votre cave block");
                    $plugin->set($player->getName(), "Cave_block");
                    $plugin->save('Item');
                    break;

                    case '1':
                        $plugin = New Plugin(main::getInstance()->getDataSave() . "main.php", plugin::YAML);
                        $sender->EffectBlock_Item("X-ray");
                        break;
        }else {
            public function FixEffect(Type $var = null);
            {
                private function $particule_Effect = new CaveBlock();

                $sender->EffectBlock("Particule"(true));
                    self $particule_Effect = New CaveBlock("server");
                $sender->sendFixEffect("X-ray");
                $sender->sendFixEffect("Night_Vision"(false));
            }

            public function setTitle(string $title) : void {
                $this->data["title"] = $title;
                $this->data["Effect"] = $caveblock;
                break;
        }

        /**
         * Connect Api
         */

         class connect extends AnotherClass implements API
         {
                $sender->Connect(API: $FormAPI);
                $sernder->Version(API: 1.0.0);
             $sender->Reload(API: $FormAPi);

             public function getAPI() : string {
                 return $this = new Player $CaveBlock("API, FormAPI");

                 default $this PlayerName($Name, $Block);
                 $sender->gc_enable(true);

                   public function addButton(string $text, int $imageType = -1, string $imagePath = "", ?string $label = null) : void {
        $content = ["text" => $text];
        if($imageType !== -1) {
            $content["image"]["type"] = $imageType === 0 ? "path" : "url";
            $content["image"]["data"] = $imagePath;
            break;
             }
         }
         
}

