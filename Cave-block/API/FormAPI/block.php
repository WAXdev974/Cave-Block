<?php

namespace \CaveBlock\block;

use pocketmine\plugin\Cave_Block;
use pocketmine\Kryptonmc\API;
use pocketmine\Kryptonmc\FormAPI;
use pocketmine\Kryptonmc\Item;
use pocketmine\Player;
use pocketmine\Server;
use pocketmine\Kryptonmcpe\block;
use pocketmine\block\Item;

/**
 * Block API
 */

 class Block extends AnotherClass implements APi
 {
    $sender->BlockAPI("FormAPI"); 
    $block = new Item;
    $sender->BlockAPI(static, array);
private function $PL onEnable("Item");
    $sender->snedblock(static, array);

    self::$set $PlayerName->onBlock("FormAPI");
$sender->sendKB(bool) $PlayerName->getserver(static, array);

self $Playerserver->sem_release(true, array);
   public static function $PlayerName->session_encode(true);
   return $blockAPI->session_id(static, array, bool);
   return $sender->BlockId("84"); // Block API ID
   return $blockName->idName("CaveBlock");
    break;
 }

 /**
  * API
  */

  class API extends AnotherClass implements Plugin
  {
        public function $API = var <object $API("Item");
      $sender->API(static, bool);
      self $PlayerClass ($idName = new ShopAPI);
      $sender->sendMessage("tu as reçue l'effect cave block...") $display on(off);

      public static function open(Player $p)
      {
          {
              $world = [];

              foreach (Server::getInstance()->getLevels() as $level){
                array_push($world,$level->getName());

                $FormAPI = new case 'value':
                    self::$set === $data Block("");
                    break;
  }